import styled from "styled-components";

const StyledCommunity = styled.div`
  width: 100%;
  height: auto;
  display: flex;
  gap: 3px;
  overflow-y: auto;
  overflow-x: hidden;
`;

export { StyledCommunity };
