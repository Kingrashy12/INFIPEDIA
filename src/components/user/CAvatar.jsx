import React from "react";
import { CommentsAvatar } from "../../styles/components/models/postcomments";
import { Male } from "../../asset";

const CAvatar = ({ src }) => {
  return <CommentsAvatar src={src} />;
};

export default CAvatar;
